# 本次安全测试任务

> 由用户在每次任务开始前填写本文件，技能将以此为起点进行子任务规划。

## 测试目标

（待填写：目标系统名称、访问入口、版本等）

## 测试范围

（待填写：需要测试的功能点、接口、页面列表；以及明确排除的范围）

## 约束与偏好

（待填写：例如"禁止注册新用户"、"仅允许读操作"、"每个用例限 2 次尝试"等）

## 输入产物

（待填写：若已具备源码路径、接口文档、登录凭据、抓包记录等，请列出对应路径或位置）

## 要执行的用例

> 可选。若填写本节，skill 会在启动时按顺序把指定用例的 `说明.md` 作为业务背景加载；
> 攻击模式的选择仍按 `核心策略/主策略.md` 的流程动态进行。
> 不填则跳过此阶段，按默认流程测试。

用例:
  # - 上传业务配置ZIP
  # - 带验证码的登录
