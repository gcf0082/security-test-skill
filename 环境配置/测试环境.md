# 测试环境配置

## 运行环境
- 访问入口：`http://1.1.1.1:8008`
- 测试账号：`admin` / `test11234`

## 源码路径（白盒测试用）
- **默认**：项目内 `项目代码/`（单项目，单根；用户克隆 / 拷贝到此即可）。
- **覆盖**：若源码在 skill 项目外的其他位置，在此填绝对路径覆盖默认值。
- **当前覆盖**：（无）

## 说明
- 以上为示例值，实际任务开始前请由用户覆盖为真实目标。
- 若 `项目代码/` 为空且本节"当前覆盖"也无值，则跳过 `攻击模式库/白盒攻击模式/`，仅进行黑灰盒测试。
- 凭据仅限本 skill 与测试环境使用，严禁用于其他用途。
