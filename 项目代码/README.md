# 项目代码

被测项目源码的固定存放位置——克隆或拷贝到本目录后，白盒 / 黑白盒结合模式的 subagent 会直接读这里。

- **单项目存放**：本目录直接是源码根，切换被测项目时清空后重新放入。
- **git 忽略**：本目录下除本 README 之外的所有文件均被 `.gitignore` 排除，不会进入 skill 仓库。
- **覆盖**：若源码在 skill 项目外的其他绝对路径，可在 `环境配置/测试环境.md` 的"源码路径"小节填入覆盖值。subagent 路径解析顺序为：任务描述显式路径 > `测试环境.md` 覆盖值 > 本目录（默认）。
